/**
 * Format transitions tokens
 * @param {Object} tokens transition tokens
 * @return {Object} Formatted transitions tokens
 */
const transitions = (tokens) => {
  return {
    fast: 'transform .125s ease, opacity .125s ease',
  };
};

module.exports = transitions;
