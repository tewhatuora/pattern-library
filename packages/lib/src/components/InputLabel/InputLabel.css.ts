import { style } from '@vanilla-extract/css';

export const label = style({
  display: 'flex',
  justifyContent: 'space-between',
});

export const tertiaryLabelButton = style({});

export const headingWrapper = style({});
